/// Central place for initializing and configuring Firebase.
///
/// I’ll call `FirebaseConfig.initialize()` from main() before runApp(),
/// once I add Firebase and related configuration.
class FirebaseConfig {
  static Future<void> initialize() async {
    // TODO: add Firebase.initializeApp and any other setup here.
  }
}